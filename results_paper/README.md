# results_paper

This directory contains the experimental results reported in the paper.

The results correspond to the outputs obtained by running:

```
python REDI.py --model all --dataset all --protected all --runs 10
```


In the paper, only four selected scenarios are presented for each research question (RQ).  
The complete results for all 12 scenarios are available in `Results_for_all_tasks.pdf`.